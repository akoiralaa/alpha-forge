# Credentials template — NEVER commit actual credentials to git.
# Copy this to credentials.yaml and fill in your values.
# credentials.yaml is in .gitignore.

ibkr:
  host: "127.0.0.1"
  port: 7497              # 7497 for paper, 7496 for live
  paper: true
  client_id: 1

polygon:
  api_key: "YOUR_POLYGON_API_KEY"
  rate_limit_per_minute: 5  # free tier = 5, basic = 100

alpaca:
  api_key: "YOUR_ALPACA_API_KEY"
  api_secret: "YOUR_ALPACA_SECRET"
  paper: true
